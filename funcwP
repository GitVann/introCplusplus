// defining a function with parameter

#include <cstdio>
using namespace std;

void function(int n) {
    printf("value n is: %d\n", n);
}

int main(int argc, char ** argv) {
    puts("this is main()");
    function(5);
    function(10);
    function(100);
    return 0;
}
